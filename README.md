# LeadGate
Lead scoring for bank telemarketing: predicts who subscribes to a term deposit, trained on 45k real calls (UCI Bank Marketing) and served via S3 → Lambda → API Gateway.    ▎ Built on one constraint — no feature the call centre couldn't know before dialling, which rules out the dataset's strongest predictor.
